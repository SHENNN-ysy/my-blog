# 请复制此文件为 application-local.yml 并填入实际值
# 此文件不要提交到版本库

spring:
  datasource:
    username: root
    password: your_db_password_here

jwt:
  secret: your_jwt_secret_here_must_be_at_least_32_chars
  expiration: 86400000  # 24 hours in milliseconds
