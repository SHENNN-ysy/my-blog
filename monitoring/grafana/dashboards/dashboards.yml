apiVersion: 1

providers:
  - name: 'my-blog'
    uid: my-blog-dashboards
    folder: Blog
    folderUid: blog
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
